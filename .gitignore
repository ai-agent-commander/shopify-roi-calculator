# ========================================
# ROOT .gitignore for Shopify ROI Calculator
# ========================================
# /src = Next.js source code (commit this)
# /docs = Built static HTML (commit this for GitHub Pages)

# Dependencies (don't commit)
src/node_modules/
node_modules/
package-lock.json
src/package-lock.json

# Testing artifacts (don't commit)
src/coverage/
src/playwright-report/
src/test-results/
src/.playwright/
coverage/

# Next.js build cache (don't commit)
src/.next/
.next/

# ⚠️ IMPORTANT: /docs is NOT ignored
# This folder contains the static HTML build for GitHub Pages and MUST be committed

# TypeScript build cache (don't commit)
*.tsbuildinfo
src/*.tsbuildinfo

# Environment files (never commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
src/.env
src/.env.*

# Logs (don't commit)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS files (don't commit)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files (don't commit)
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files (don't commit)
*.tmp
*.temp
.cache/
.parcel-cache/

# Build caches (don't commit)
.npm
.eslintcache
.stylelintcache

# Deployment platforms (don't commit)
.vercel

# Other generated files
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
pids
*.pid
*.seed
*.pid.lock
